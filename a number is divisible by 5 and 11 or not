<?php
	
	//Write a C program to check whether a number is divisible by 5 and 11 or not.
	
	$num = readline('Enter the number : ');

	if (($num % 5==0) && !($num % 11)) {
		
		echo "number is divisible by 5 and 11 : ". $num;
	}
	else {

		echo "number is not divisible by 5 and 11 : ". $num;
	}

?>
